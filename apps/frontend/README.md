# madchatter - frontend

Official package for the Mad Chatter frontend. Hosts both the admin pages and the viewer page.

## Getting Started

We use npm as the package manager.

First, call `npm run build` to fetch the static files needed for `vad-react` to work.

Then, run the development server:

```bash
npm run dev
```
